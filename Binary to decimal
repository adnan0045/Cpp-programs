#include <iostream>
using namespace std;
void biToDec(int biNum){
    int n=biNum;
    int dec=0;
    int po=1;
    while (n>0){
        int dig=n%10;
        dec+=dig*po;
        po=po*2;
        n/=10;
    }
    cout<<dec<<endl;
}
int main (){
   biToDec(101);
    return 0;
}
